apiVersion: v1
kind: LimitRange
metadata:
  name: default-limitrange
spec:
  limits:
  - defaultRequest: # this section defines default requests
      cpu: 128mi
      memory: 128Mi
    type: Container
